spring:
  application:
    name: rest-with-string-boot-and-java-erudio
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_string_boot_rodrigo?useTimezone=true&serverTimezone=UTC
    username: root
    password: RNS!sd11
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: false
    open-in-view: false
logging:
  level:
    root: INFO
    #br.com.nsrodrigo: DEBUG